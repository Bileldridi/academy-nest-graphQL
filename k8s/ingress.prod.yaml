apiVersion: extensions/v1beta1
kind: Ingress
metadata: 
    annotations:
        kubernetes.io/ingress.class: nginx
        kubernetes.io/tls-acme: "true"
    labels:
        app: production
        chart: auto-deploy-app-0.2.9
        heritage: Tiller
        release: production
    name: academy-backend-ingress-prod-__CI_ENVIRONMENT_SLUG__
    namespace: academy-backend
spec:
    rules:
    - host: backend.academy.fivepoints.fr
      http:
        paths:
        - backend:
            serviceName: academy-backend-prod-__CI_ENVIRONMENT_SLUG__
            servicePort: 80
          path: /
    tls:
    - hosts:
      - backend.academy.fivepoints.fr
      secretName: __CI_ENVIRONMENT_SLUG__-backend-auto-deploy-tls
